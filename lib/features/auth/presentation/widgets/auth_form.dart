import 'package:flutter/material.dart';

/// Auth form widget placeholder.
class AuthForm extends StatelessWidget {
  const AuthForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Auth Form Placeholder');
  }
}
