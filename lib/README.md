# CampusPulse app architecture

This folder contains the main app entry point and feature-based architecture for CampusPulse.
Use `core/`, `features/`, `shared/`, and `routes/` for clean, scalable development.
